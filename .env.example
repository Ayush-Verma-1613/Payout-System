# Copy this file to `.env` before running the project.

# Database connection. Default is a local, zero-setup SQLite file.
# To use PostgreSQL instead, change the datasource provider in
# prisma/schema.prisma to "postgresql" and set a Postgres URL here.
DATABASE_URL="file:./dev.db"

# HTTP port for the API server
PORT=3000
